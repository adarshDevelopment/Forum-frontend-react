import React from 'react'

function Login({ className }) {
    return (
        <div className={`h-[100vh] min-h-screen max-h-screen w-full bg-red-40 ${className}`}>
            inside login
        </div>
    )
}

export default Login